git --version
heroku -v

git init -create .git directory

git status

git add folderName/ || git add . -> for all files
git commit -m "comment"


-----------------------
 **  HEROKU  **

COMMANDS 
  heroku keys:add  -> adds ssh key
  